[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "vewutils"
authors = [
    {name = "Shintaro Bunya", email = "sbunya@gmail.com"}
]
description = "Utility programs for the ADCIRC hydrodynamic model"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11,<3.12"
# Note: NCO tools (nco package) are recommended for optimal performance in rechunk-netcdf command
# but are optional - the command will fall back to Python implementation if NCO is not available
dependencies = [
    "pandas",
    "numpy",
    "shapely",
    "plotly",
    "geopandas",
    "scipy",
    "geopy",
    "rasterio",
    "rasterstats",
    "pyproj>=2.6",
    "pyyaml",
    "matplotlib",
    "folium",
    "xarray",
    "requests",
    "pytz",
    "jupyter",
    "ipywidgets",
    "netCDF4",
    "haversine",
    "paramiko",
    "pooch",
    "psutil",
    "searvey",
    "tqdm",
    "utm",
    "appdirs"
]

[project.optional-dependencies]
docs = [
    "sphinx>=7.0",
    "sphinx-rtd-theme",
    "sphinx-autodoc-typehints",
    "myst-parser"
]

[project.scripts]
vewutils = "vewutils.cli:main"
